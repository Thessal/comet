Behavior Comparator(a, b) -> Series

Implementation Ratio implements Comparator(a, b) where b is NonZero {
    return divide(a, b)
}

Flow Test {
    x <- Price
    res <- Comparator(x, 2)
    return res
}
