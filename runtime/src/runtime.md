### Notation 

We use reverse polish notation, represented in list of 
* literals(in String)
* operator(program::TypeDecl)


### 
TODO : define SyntaxTreeElem enum 
enum SyntaxTreeElem {
    Literal(String), 
    Operator(TypeDecl)
}